# Voxel Survival Universe 20

Optimized mobile build. Universe 20 fixes the Play/startup path: iOS Safari orientation locking can no longer block startup, the first chunk is generated locally, audio failures are non-fatal, and worker generation has a timeout fallback.

Upload the contents of this folder to the GitHub Pages root.
